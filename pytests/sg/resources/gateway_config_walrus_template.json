{
    "adminInterface": "LOCAL_IP:4985",
    "interface": "LOCAL_IP:4984",
    "log": ["HTTP", "HTTP+", "CRUD"],
    "databases": {
        "db": {
            "server": "walrus:",
            "users": {
                 "GUEST": {"disabled": false, "admin_channels": ["*"] }
            }
        }
    }
}

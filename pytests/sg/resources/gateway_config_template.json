{
	"log": ["HTTP", "HTTP+", "CRUD", "Feed", "Bucket"],
    "adminInterface": ":4985",
    "databases": {
        "db": {
            "server": "http://{{password}}{{db_ip}}:{{port}}/",
            "bucket": "{{bucket}}",
            "feed_type":"{{feedtype}}",
            "users": {
				"GUEST": {"disabled": false, "admin_channels": ["*"] }
            }
        }
    }
}

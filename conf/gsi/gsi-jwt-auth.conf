gsi.gsi_jwt_auth.GSIJWTAuthTests:

    # --- PEM key (locally generated RSA key pair) ---
    test_pem_jwt_indexer_endpoints,nodes_init=2,services_init=kv:n1ql-index,default_bucket=True,create_primary_index=False,skip_load=True,GROUP=pem
    test_pem_jwt_indexer_endpoints,nodes_init=3,services_init=kv:n1ql-index-index,default_bucket=True,create_primary_index=False,skip_load=True,GROUP=pem

    # --- JWKS JSON key (local RSA key exported as JWK set) ---
    test_jwks_json_jwt_indexer_endpoints,nodes_init=2,services_init=kv:n1ql-index,default_bucket=True,create_primary_index=False,skip_load=True,GROUP=jwks_json
    test_jwks_json_jwt_indexer_endpoints,nodes_init=3,services_init=kv:n1ql-index-index,default_bucket=True,create_primary_index=False,skip_load=True,GROUP=jwks_json

    # --- External IDP (Keycloak, realm: cb-index, client: index-client public, user: jit_user) ---
    test_idp_jwt_indexer_endpoints,nodes_init=2,services_init=kv:n1ql-index,default_bucket=True,create_primary_index=False,skip_load=True,keycloak_realm=cb-index,keycloak_client_id=index-client,GROUP=idp
    test_idp_jwt_indexer_endpoints,nodes_init=3,services_init=kv:n1ql-index-index,default_bucket=True,create_primary_index=False,skip_load=True,keycloak_realm=cb-index,keycloak_client_id=index-client,GROUP=idp

    # --- Negative: expired JWT, JWT disabled, tampered tokens ---
    test_negative_jwt_scenarios,nodes_init=2,services_init=kv:n1ql-index,default_bucket=True,create_primary_index=False,skip_load=True,GROUP=negative

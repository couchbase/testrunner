xdcr.xdcrP2PCheckpointTests.XDCRP2PCheckpointTests:
    # Test P2P checkpoint functionality during rebalance operations
    test_p2p_checkpoint_during_rebalance,items=1000,rdirection=unidirection,ctopology=chain,checkpoint_interval=120,default_bucket_name=default,GROUP=P1

    # Simple P2P checkpoint validation test
    test_p2p_checkpoint_simple_validation,items=100,rdirection=unidirection,ctopology=chain,checkpoint_interval=60,default_bucket_name=default,GROUP=P0

    # P2P checkpoint test with higher load
    test_p2p_checkpoint_during_rebalance,items=2000,rdirection=unidirection,ctopology=chain,checkpoint_interval=180,default_bucket_name=default,GROUP=P2 
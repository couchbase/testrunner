xdcr.conflictLoggingTests.ConflictLoggingTests:
    # Basic conflict logging test with default scope/collection
    test_conflict_logging_basic,items=100,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,batch_size=10,throughput_concurrency=4,GROUP=P0
    
    # Conflict logging with custom scope/collection
    test_conflict_logging_basic,items=100,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,batch_size=10,throughput_concurrency=4,GROUP=P0
    
    # Multiple buckets with different conflict logging settings
    test_conflict_logging_basic,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,standard_buckets=1,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,standard_bucket_1@C1=conflict_logging:testscope.testcollection,GROUP=P0
    
    # Test updating conflict logging settings
    test_conflict_logging_update_settings,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P1
    
    # Conflict logging with operations
    test_conflict_logging_basic,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,doc_ops=update-delete,doc_ops_clusters=C1:C2,default@C1=conflict_logging:testscope.testcollection,GROUP=P1
    
    # Conflict logging with cluster operations
    test_conflict_logging_basic,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,rebalance_in=C1:C2,default@C1=conflict_logging:testscope.testcollection,GROUP=P1
    
    # Conflict logging with pause-resume
    test_conflict_logging_basic,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,pause=C1:C2,default@C1=conflict_logging:testscope.testcollection,GROUP=P1
    
    # Conflict logging with failover
    test_conflict_logging_basic,items=1000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,failover=C1:C2,graceful=True,default@C1=conflict_logging:testscope.testcollection,GROUP=P1
    
    # ==================== STRESS TEST CASES ====================
    
    # High-Volume Conflict Test - stress test with 50k docs in default collection
    test_conflict_logging_high_volume,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P2
    
    # High-Volume Conflict Test with custom collection
    test_conflict_logging_high_volume,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P2    
    
    
    # ==================== TOPOLOGY CHANGE TESTS ====================
    
    # Test conflict logging during rebalance
    test_conflict_logging_during_rebalance,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P2
    
    # Test conflict logging during rebalance with custom collection
    test_conflict_logging_during_rebalance,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P2
    
    # Test high volume conflicts during rebalance
    test_conflict_logging_high_volume,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,rebalance_in=C1,default@C1=conflict_logging:testscope.testcollection,GROUP=P3
    
    # ==================== MULTI-BUCKET STRESS TESTS ====================

    # High-Volume Test with multiple buckets
    test_conflict_logging_high_volume,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,standard_buckets=1,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,standard_bucket_1@C1=conflict_logging:testscope.testcollection,GROUP=P3

    # ==================== AUTO-PAUSE (cLogPauseReplThreshold / cLogMonitorDuration) ====================

    # Settings validation: accepted ranges + rejection of out-of-range / non-integer values
    test_clog_pause_settings_validation,items=100,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,GROUP=P0

    # Functional: conflicts above threshold within window auto-pause the replication + errors-API reason
    test_clog_autopause_on_threshold_breach,items=2000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=1,clog_monitor_duration=30,GROUP=P0

    # Mutual exclusivity: defaults (0,0) keep legacy hibernation -> no auto-pause under load
    test_clog_default_hibernation_unchanged,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=0,clog_monitor_duration=0,GROUP=P0

    # Live update: high threshold (no pause) then lowered threshold (auto-pause) on running pipeline
    test_clog_pause_settings_live_update,items=2000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=1,clog_monitor_duration=30,GROUP=P1

    # Window reset: sustained sub-threshold conflict rate across windows must not auto-pause
    test_clog_window_reset,items=2000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=1000,clog_monitor_duration=10,GROUP=P1

    # Recovery: resume after an auto-pause and confirm replication catches up
    test_clog_resume_after_autopause,items=2000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=1,clog_monitor_duration=30,GROUP=P1

    # Topology: auto-pause still fires when conflict burst occurs during a source rebalance
    test_clog_autopause_during_rebalance,items=5000,enable_cross_cluster_versioning=True,rdirection=bidirection,ctopology=chain,replication_type=xmem,default@C1=conflict_logging:testscope.testcollection,clog_pause_threshold=1,clog_monitor_duration=30,GROUP=P2

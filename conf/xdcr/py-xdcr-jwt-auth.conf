xdcr.xdcr_jwt_auth.XDCRJWTAuth:

    # Positive: JWT admin drives the full REST lifecycle
    test_jwt_admin_full_replication_lifecycle,ctopology=chain,enable_dp=True

    # Negative: JWT user without XDCR role is forbidden
    test_jwt_user_without_xdcr_permission_gets_forbidden,ctopology=chain,enable_dp=True

    # Negative: expired / tampered JWTs are rejected
    test_malformed_expired_tampered_jwt_rejected,ctopology=chain,enable_dp=True

    # Coexistence: Basic Auth and JWT across one replication's lifecycle
    test_basic_auth_and_jwt_coexist_across_replication_lifecycle,ctopology=chain,enable_dp=True

    # Regression: JWT config enabled must not break P2P replication
    test_jwt_config_enabled_does_not_break_p2p_replication,ctopology=chain,rdirection=bidirection,items=1000,enable_dp=True,update=C1-C2,delete=C1-C2

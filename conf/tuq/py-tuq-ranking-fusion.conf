tuqquery.tuq_ranking_fusion.QueryRankingFusionTests:
    # Algorithm validation against the test plan's worked examples
    test_rrf_dummy_data,GROUP=algorithm;P0
    test_rsf_dummy_data,GROUP=algorithm;P0

    # RRF over real FTS + vector data
    #test_rrf_fts_dense,GROUP=rrf;fts;dense;P0
    #test_rrf_fts_sparse,GROUP=rrf;fts;sparse;P0
    test_rrf_dense_sparse,GROUP=rrf;dense;sparse;P0
    #test_rrf_three_inputs_fts_dense_sparse,GROUP=rrf;fts;dense;sparse;P1

    # RSF over real FTS + vector data
    #test_rsf_fts_dense,GROUP=rsf;fts;dense;P0
    #test_rsf_fts_sparse,GROUP=rsf;fts;sparse;P0
    test_rsf_dense_sparse,GROUP=rsf;dense;sparse;P0

    # Normalization methods
    test_normalization_none,GROUP=normalization;P1
    test_normalization_sigmoid,GROUP=normalization;P1
    test_normalization_minmaxscaler,GROUP=normalization;P1

    # Options-object behavior
    test_score_flag_true_emits_component_scores,GROUP=options;P1
    test_score_flag_false_omits_component_scores,GROUP=options;P1
    test_limit_caps_results,GROUP=options;P1
    test_weight_skews_ordering,GROUP=options;P1
    test_penalty_changes_rrf_ranking,GROUP=options;P1
    test_aggregator_sum_dedupes_within_input,GROUP=options;aggregator;P1
    test_aggregator_max_dedupes_within_input,GROUP=options;aggregator;P1
    test_pathid_pathscore_custom_fields,GROUP=options;P1

    # CTE-bound input arms (WITH q1 AS (...), q2 AS (...) ...)
    test_rrf_dummy_via_cte,GROUP=cte;algorithm;P0
   # test_rrf_fts_dense_via_cte,GROUP=cte;rrf;fts;dense;P0
    test_rrf_dense_sparse_via_cte,GROUP=cte;rrf;dense;sparse;P0
    test_rsf_dense_sparse_via_cte,GROUP=cte;rsf;dense;sparse;P0
    #test_rrf_three_inputs_via_cte,GROUP=cte;rrf;fts;dense;sparse;P1
    #test_rrf_fts_dense_filtered_via_cte,GROUP=cte;filters;P1

    # Fusion with WHERE-clause filters on auxiliary fields
    #test_rrf_fts_dense_filtered_by_price_present,GROUP=filters;P1
    test_rrf_dense_sparse_filtered_by_price_range,GROUP=filters;P1
    #test_rrf_fts_dense_brand_size_composite,GROUP=filters;P1
    test_rrf_dense_sparse_new_brand_filter,GROUP=filters;P1
    test_rrf_sparse_nested_array_any,GROUP=filters;nested;P1
    #test_rsf_fts_dense_null_field_present,GROUP=filters;nulls;P1
    test_rrf_dense_sparse_nested_scalar,GROUP=filters;nested;P1
    #test_rrf_three_inputs_all_filtered,GROUP=filters;P1

    # Negative cases
    test_invalid_scorer,GROUP=negative;P1
    test_invalid_normalization,GROUP=negative;P1
    test_invalid_aggregator,GROUP=negative;P1
    test_invalid_fusion_type,GROUP=negative;P1
    test_missing_options_argument,GROUP=negative;P1
    test_no_input_subqueries,GROUP=negative;P1
    test_negative_limit,GROUP=negative;P2
    test_negative_penalty,GROUP=negative;P2
    test_non_boolean_score_flag,GROUP=negative;P2
    test_pathid_points_at_missing_field,GROUP=negative;P2

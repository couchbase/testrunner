#Cluster wide monitoring
tuqquery.tuq_monitoring.QueryMonitoringTests:
    # Config Tests
    test_set_completed_config
    test_retention_config
    test_collection_config
    # Normal Query Tests
    test_purge_completed
    test_simple_cluster_monitoring
    test_filter_by_node
    test_server_failure
    # Prepared Tests
    test_prepared_deletion,named_prepare=prepared_delete
    test_prepared_monitoring,named_prepare=prepared_monitoring
    test_prepared_filtering,named_prepare=prepared_filtering
    test_prepared_check_requestId,named_prepare=prepared_requestId
    test_prepared_kill_by_requestId,named_prepare=prepared_kill_requestId
    test_prepared_kill_by_name,named_prepare=prepared_kill_request_name
    test_prepared_kill_by_node,named_prepare=prepared_kill_request_node
fts.stable_topology_fts.StableTopFTS:

    # --- CT: Configuration / Toggle Tests ---
    test_udf_ct,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable

    # --- CF: custom_filter Functionality ---
    test_udf_cf_basic,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable
    test_udf_cf_advanced,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- CS: custom_score Functionality ---
    test_udf_cs_basic,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable
    test_udf_cs_advanced,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- FL: Fields Loading Tests ---
    test_udf_fl,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- P: Params Tests ---
    test_udf_params,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- JS: JavaScript Contract Tests ---
    test_udf_js,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable

    # --- QC: Query Composition Tests ---
    test_udf_qc_basic,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable
    test_udf_qc_advanced,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- EH: Error Handling & Failure Semantics ---
    test_udf_eh_validation,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable
    test_udf_eh_runtime,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable

    # --- SR: Security / Restriction Tests ---
    test_udf_sr_sandbox,cluster=F,F,custom_queries_enabled=true,GROUP=P0;stable
    test_udf_sr_resilience,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

    # --- PF: Performance / Resource Tests ---
    test_udf_pf,cluster=F,F,custom_queries_enabled=true,GROUP=P1;stable

fts.moving_topology_fts.MovingTopFTS:

    # --- DN: Distributed / Multi-Node Tests ---
    # D,D,F,F = 2 KV + 2 FTS nodes
    test_udf_dn_1,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P1;moving
    test_udf_dn_2,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P1;moving
    test_udf_dn_3,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P2;moving
    test_udf_dn_4,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P2;moving
    test_udf_dn_5,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P1;moving
    test_udf_dn_6,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P1;moving
    test_udf_dn_7,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P1;moving
    test_udf_dn_8,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P2;moving
    test_udf_dn_9,cluster=D,D,F,F,num_nodes=4,custom_queries_enabled=true,GROUP=P2;moving

fts.upgrade_fts.UpgradeFTS:

    test_udf_online_upgrade,cluster=F,F,F,nodes_init=4,GROUP=P0;upgrade